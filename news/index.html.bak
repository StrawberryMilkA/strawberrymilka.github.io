---
layout: default
pagination:
  enabled: true
  collection: news
  per_page: 2
  sort_field: 'date'
  sort_reverse: true
  permalink: '/news/page/:num/'
active_nav: news
extra_head: |
  <link rel="stylesheet" href="/assets/css/news.css">
---

<div style="padding:100px; background:white;">
  <h1>新闻索引测试</h1>
  <p>文档总数：{{ site.news | size }}</p>
  <p>分页页码：{{ paginator.page }} / {{ paginator.total_pages }}</p>
  <p>本文档数：{{ paginator.documents | size }}</p>

  {% for doc in paginator.documents %}
    <div style="border:1px solid #ccc; margin:10px; padding:10px;">
      <h3>{{ doc.title }}</h3>
      <p>{{ doc.date }}</p>
    </div>
  {% else %}
    <p>没找到文档，请检查集合和分页配置。</p>
  {% endfor %}
</div>